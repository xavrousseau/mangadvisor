[project]
name = "mangadvisor"
version = "0.1.0"
description = "Moteur de recommandation de mangas"
requires-python = ">=3.10"

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.pytest.ini_options]
testpaths = ["tests"]